# 物理顾问 PhysicsAdvisor · 每日巡检报告

> 巡检时间:2026-08-27 02:20 CST
> 巡检路径:`/Users/aaron/Mac/Consultant/15-物理-Physics/_PhysicsLib/PhysicsWeb/`
> Git 分支:main · HEAD:`90e248b` · 上次巡检:`7992165` (2026-08-26)

---

## 一、巡检项(5 项)

### 1. Phase 0 进展 3/8 · 关系层就位但公式层被原料卡住 ⏳
- ✅ **关系层已就位**:`md_to_json.py` 升级到 schema 0.3.0,`data/knowledge.json` 实测 **10 topics / 194 concepts / 389 relations / 10 branches**;4 类边(`branch_topic`×10 / `topic_concept`×194 / `parent_child`×123 / `cross_ref`×62)数据齐整(2026-08-26 03:24 `90e248b`)
- ⏳ **公式层被原料卡住(关键发现)**:`_PhysicsLib/物理公式/` 目录存在但**仅含 `.` 与 `..`,0 个 md 文件**;计划目标 100+ 公式,但原料为空,直接升级 `md_to_json.py` 加 `formulas` 数组没有上游可解析
- ⏳ **5 项待启动**:公式层(无原料) / 案例层(20+ 应用案例) / 公式库 LaTeX 化 / 经典实验清单(30+) / 飞书 Bot 公式速查流程

### 2. Phase 1 仍 0/8 · Web 端 0 代码 ⏳
- 全部 8 项(Web 骨架 / 图谱可视化 / 概念可视化 / 公式速查 / 交互实验 / 案例页 / 飞书 OAuth / Docker Compose)处于 0 状态,无变化
- `PhysicsWeb/` 仍只有 `scripts/md_to_json.py` + `data/knowledge.json` + 2 个 md,无 `backend/` `frontend/` `deploy/` 目录;连续 2 天建议"先做 FastAPI + Vite React 最小骨架",未启动
- 现在 `data/knowledge.json` 已有 194 概念 + 389 关系,完全够支撑一个**只读图谱浏览页**起步,门槛比昨日更低

### 3. .plan 节奏断档 3 天 ⚠️(持续恶化)
- `.plan/` 仍**只有 `.DS_Store` 一个空文件**,既无 `20260825.md` / `20260826.md` / `20260827.md`
- 0826 巡检 P0 建议"补 `.plan/20260826.md`",0827 仍未补;连续 3 天断档,T1 火车站调度模型(78 档 × 7 槽)对本顾问的编排连续性已实质失效
- 建议 T1 任务直接生成"0825-0827 三日补录 + 0827 当日 5 任务"4 份 `.plan`,一次补回

### 4. 设计文档 `物理顾问开发架构与计划.md` 仍 untracked ⚠️(连续 2 天)
- 唯一未跟踪文件仍为该 18.5 KB 文档(连续 2 天不变,0825→0826→0827),与 `项目开发计划.md` 内容 70% 重复
- 0826 巡检建议"决策去留(合并 / 附录 / 删除)",T1 阶段无回应;若再不决,0828 巡检将继续标 ⚠️
- 巡检只报告不修改,本项持续挂账风险上升

### 5. `.gitignore` case-insensitive 仍需 `git add -f` ⚠️(依赖人工绕开)
- `.gitignore:2:/*.log` 在 macOS APFS/HFS+ 默认 case-insensitive 下会匹配 `.Log/`,导致本次 `git add .Log/巡检-物理-20260827.md` 仍被拒,继续靠 `git add -f` 入库
- 0826 巡检 P2 建议改 `**/*.log` + `!.Log/**` 或重命名 `.Log → logs/`,未修;本次 commit 同样需 `-f`
- 远程双备(gitee + github)正常,不影响推送,但属于长期债

---

## 二、行动建议(非强制)

| 优先级 | 建议 | 依据 |
|--------|------|------|
| **P0** | **公式层原料问题先解决**:从 10 主题 md 抽取"经典公式"段落(牛顿三定律/麦克斯韦方程/薛定谔方程等)先凑 20+ 公式,或由张勇手工提供 50 个核心公式 md 入库 `_PhysicsLib/物理公式/`;同时建"案例 + 实验"原料采集,不要让 `md_to_json.py` 升级到 0.4.0 之后跑空 | 公式层是 Phase 0 最后一道关键门槛,空目录直接阻塞图谱"可查询" |
| **P0** | 补 `.plan/20260825.md` / `20260826.md` / `20260827.md` 三日补录,恢复 T1 节奏 | 已断档 3 天,再不补 T1 火车站调度模型对本顾问的 5 任务编排将彻底失效 |
| **P0** | 决策 `物理顾问开发架构与计划.md`:推荐**合并到 `项目开发计划.md` 升级 v2.0**(保留两文档差异点,统一口径),不再单立 | 连续 2 天不决,继续挂账会让"两份计划漂移"风险倍增 |
| **P1** | 启动 Phase 1 最小骨架:`backend/`(FastAPI 单文件 + `/health` + `/graph` 读 knowledge.json 194 概念) + `frontend/`(Vite React 首页),本周内跑通 | data/knowledge.json 已就位 194 概念,门槛是历史最低 |
| **P2** | 修 `.gitignore`:`/*.log` 改 `**/*.log` + `!.Log/**` 或重命名 `.Log → logs/`,移除 `git add -f` 依赖 | 持续 2 天债,不复杂,顺手可解 |

---

## 三、巡检元数据

- **本次巡检项数**:5
- **报告路径**:`.Log/巡检-物理-20260827.md`
- **Commit Hash**:(见本次 commit 输出)
- **下次巡检**:2026-08-28 02:20 CST(系统 cron 自动触发)
- **巡检未修改**:`项目开发计划.md` 主文件保持原样

---

> 巡检只报告,不修改 `项目开发计划.md` 主文件。
