# PhysicsAdvisor 2026-09-08 当日开发计划 (T5)

> 触发:系统 cron 03:20 T5 自动执行
> 状态:本计划由 T5 自我生成(0908 plan 由 T4 漏写,沿用 0901/0903/0905/0907 同口径"T5 自填")
> 上次 commit:`c7d0e81` (inspect 0908 巡检)
> 0908 巡检:`.Log/巡检-物理-20260908.md` · 5 项巡检,**Phase 1 0/9 连续 14 天升级 🔴🔴🔴 P0** + 触发 PROGRESS_STOPPED
> 当日目标:1 个小变更(简洁可入库)· 优先级 P0 破冰

---

## 一、当日挂账(来自 0908 巡检)

1. 🔴🔴🔴 P0:Phase 1 0/9 连续 14 天(0826→0908),0907 巡检已升级阻塞,0908 仍零业务 commit,触发 PROGRESS_STOPPED
2. 🟡 P1:LaTeX 化 2/5 连续 2 天未推进(0906→0908,0906 承诺"0907 热学 / 0908 光学"未兑现)
3. 🟡 P1:实验层 34 条待扩(0908 巡检建议补 16+ 推到 50+)
4. 🟡 P2:`.gitignore:3:/*.log` case-insensitive 债 14 天

---

## 二、今日变更(1 个)

**feat(skeleton): Phase 1 最小骨架破冰 — `backend/app.py` FastAPI 5 路由 + `frontend/index.html` 静态首页**

变更内容:
- 新建 `backend/app.py`(FastAPI 单文件,50 行起步),5 路由:
  - `GET /health` 健康检查(返回 schema + 资产数)
  - `GET /graph` 知识图谱(返回 concepts + relations 精简版,前 50 节点)
  - `GET /formulas` 公式列表(支持 `?branch=力学` 过滤)
  - `GET /cases` 应用案例(支持 `?category=故事` 过滤)
  - `GET /experiments` 经典实验(从 `data/experiments/*.md` 解析)
- 新建 `frontend/index.html`(D3.js v7 CDN 拉 `/graph` 渲染图谱骨架,30 行起步,首屏显示节点数 + 简单力导向图)
- 新建 `backend/requirements.txt`(`fastapi` + `uvicorn`,沿用 macOS 系统已有的 fastapi 0.128.8,无新依赖)
- 新建 `backend/README.md`(启动方式:`cd backend && uvicorn app:app --reload --port 8000`)
- 新建 `deploy/.gitkeep`(占位,留 Docker Compose 一键启动位)

对齐:
- ✅ 🔴🔴🔴 P0 Phase 1 0/9 破冰(0826→0908 跨 14 天首次有代码,数据门槛 87 公式 + 34 实验 + 29 LaTeX 已就位)
- 🟡 P1 LaTeX 化 2/5 / 实验层 34 — 留给 0909
- 🟡 P2 .gitignore — 留给后续(优先级低于破冰)

---

## 三、未解套挂账(留给后续)

- 🔴 Phase 1 8/8 — 0908 启动 1/8(本次),后续 0909 起每 T5 推 1 个
- 🟡 P1 LaTeX 化 3/5 推 03 热学 — 0909
- 🟡 P1 实验层 50+ 推 02 扩展实验 — 0910
- 🟡 P1 PROGRESS_STOPPED 事件入 CommanderDashboard — 经根 session 转交
- 🟡 P2 .gitignore 14 天债 — 0911
- .plan/ 0828/0830/0901-0907 历史断档 — T1 补录,本顾问不补

---

## 四、推送流程

1. `git add -A`(backend/ frontend/ deploy/ .plan/20260908.md / 项目开发计划.md)
2. `source /Users/aaron/Mac/Consultant/_ConsultantLib/.github-sync/env.sh`
3. `git push -q -f gitee main`
4. `git push -q -f github main`(30s 超时,失败不阻塞)
5. 报告 commit hash + Gitee 状态 + GitHub 状态

---

> 计划只列方向,不预测 commit hash;hash 在推送后回填。
