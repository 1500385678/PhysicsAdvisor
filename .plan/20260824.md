# 2026-08-24 增量开发计划

> 物理顾问 PhysicsAdvisor · T4 每日增量 · Day 1

## 选定任务
Phase 0 第 1 项:`写 md_to_json.py` —— 把已有物理 md 资产解析为结构化 JSON,作为图谱/公式/案例库的基底。

## 今日做什么
- 写 `scripts/md_to_json.py`,通用扫描 `_PhysicsLib/` 下所有主题 md(10 个 `0X_xxx/*.md` + 后续的 `物理公式/*.md`)
- 解析每个 md 为 `{id, branch, title, file_path, content, word_count, headings, first_heading, tags}` 并合并为 `data/knowledge.json`
- 验收:运行一次能成功输出 10 个主题,29084 字,字段完整

## 涉及文件
- 新增:`PhysicsWeb/scripts/md_to_json.py`(5.2 KB,可执行)
- 新增:`PhysicsWeb/data/knowledge.json`(schema 0.1.0,29084 字 / 10 主题)
- 新增:`PhysicsWeb/.plan/20260824.md`(本文件)
- 不改:`PhysicsWeb/项目开发计划.md`(T1 大计划)

## 验收
1. `python3 scripts/md_to_json.py` 退出码 0,输出 `[OK] 解析 10 个主题 md`
2. `data/knowledge.json` 含 `meta.generated_at / total_files=10 / total_words=29084`
3. `topics[0].headings` 至少有 1 个 H1,字段可被 Web 端 fetch 后按 `branch` 过滤

## 备注
原计划提到"物理知识点体系.md + 公式表/*.md",但实际资产是 10 个主题文件夹(01-10)+ 空 `物理公式/`。脚本采用**通用扫描**,后续公式库补充时直接复用同一管道,无需重写。
